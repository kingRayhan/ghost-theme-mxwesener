<div class="w-full {{post_class}}">
    <div class="relative">
        <span
            class="absolute text-xl font-bold transform -rotate-90 top-[50%] -translate-x-[50%] -translate-y-[50%] font-condensed -left-14">
            {{date format="MM YYYY"}}
        </span>


        {{#if feature_image}}
        <a href="{{url}}">
            {{!-- This is a responsive image, it loads different sizes depending on device
            https://medium.freecodecamp.org/a-guide-to-responsive-images-with-ready-to-use-templates-c400bd65c433 --}}
            <img class="w-full " srcset="{{img_url feature_image size=" s"}} 300w, {{img_url feature_image
                size="m" }} 600w, {{img_url feature_image size="l" }} 1000w, {{img_url feature_image size="xl" }} 2000w"
                sizes="(max-width: 1000px) 400px, 800px" src="{{img_url feature_image size=" m"}}" alt="{{title}}"
                loading="lazy" />
        </a>
        {{/if}}
    </div>



    <div class="mt-3 ">
        <a class="text-2xl font-bold uppercase font-condensed" href="{{ url }}">{{title}}</a>
        <p class="text-2xl font-condensed">{{excerpt}}</p>
    </div>

</div>